# Next steps (kolejnosc prac)

Krotka lista priorytetow na najblizszy czas, zebrana z brakow w `features.md` / `features_en.md`.


1) OOP / klasy: `Nazwa is a class { ... }`, singleton per nazwa, `source` jako `this`, indeksowane pola/metody.
2) Stringi: dowolna liczba cudzyslowow (w tym 0) + interpolacje (np. `${name}`, `{name}?`).
4) Indenty/whitespace: reguly 3 spacji i pelniejsze zachowanie whitespace-math z README.

